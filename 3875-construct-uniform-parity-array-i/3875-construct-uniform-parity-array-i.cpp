class Solution {
public:
    bool uniformArray(vector<int>& nums1) {
        // int n = nums1.size();
        // int minn = *min_element(nums1.begin(),nums1.end());
        // int o = INT_MAX;
        // int e = INT_MAX;
        // for(int i:nums1){
        //     if(i%2 == 0) e = min(e,i);
        //     else o = min(o,i);
        // }
        // bool f = (minn % 2 == 0);
        // for(int i:nums1){
        //     if(f){
        //         if(i%2 != 0){
        //             // if((i - o) %2 != 0) return false;
        //         }
        //     }else{
        //         if(i%2 == 0){
        //             // if((i - o) %2 == 0) return false;
        //         }
        //     }
        // }
        return true;
    }
};